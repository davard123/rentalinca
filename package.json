{
  "name": "rentalinca",
  "version": "1.0.0",
  "private": true,
  "description": "rentalinca.com website with rental estimate and inquiry backend",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check js/main.js && node --check js/components.js && node --check js/rent-data.js && node --check api/_shared.js && node --check api/rental-estimate.js && node --check api/inquiries.js && node scripts/sync-facts.js --check",
    "sync:facts": "node scripts/sync-facts.js",
    "sync:facts:check": "node scripts/sync-facts.js --check"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0"
  }
}
